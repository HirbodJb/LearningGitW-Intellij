package com.hirbod.utils;

public class Utilities {
    //just a for loop to print an array and use in Main
    public static void printAll (int [] arr){
        for (int i = 0; i < arr.length; i++ ){
            System.out.println(arr[i]);
        }
    }
}
